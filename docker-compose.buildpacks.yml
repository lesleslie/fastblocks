# FastBlocks Docker Compose with Buildpacks + OTel Stack
# This file orchestrates FastBlocks (built with buildpacks) with complete OTel infrastructure

version: '3.9'

services:
  # ==============================================================================
  # FastBlocks Web Framework (built with buildpacks)
  # ==============================================================================
  fastblocks:
    image: fastblocks:latest
    build:
      context: .
      # No Dockerfile needed! Buildpacks auto-detects and builds
      dockerfile: Dockerfile  # Will be ignored if buildpacks detect project
    environment:
      # Server configuration
      - FASTBLOCKS_ENV=production
      - FASTBLOCKS_LOG_LEVEL=INFO

      # OpenTelemetry configuration
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317
      - OTEL_SERVICE_NAME=fastblocks
      - OTEL_SERVICE_VERSION=0.1.0

      # Oneiric configuration
      - ONEIRIC_CONFIG_PATH=/app/config/production.yaml
      - ONEIRIC_LOG_LEVEL=INFO

      # Template configuration
      - FASTBLOCKS_TEMPLATE_SYSTEM=jinja2
      - FASTBLOCKS_TEMPLATE_ASYNC=true
      - FASTBLOCKS_HTMX_ENABLED=true

      # Database configuration (example)
      - DATABASE_URL=postgresql://fastblocks:fastblocks@postgres:5432/fastblocks
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      otel-collector:
        condition: service_healthy
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    ports:
      - "8000:8000"  # Web server
    networks:
      - fastblocks-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # ==============================================================================
  # FastBlocks Background Worker
  # ==============================================================================
  fastblocks-worker:
    image: fastblocks:latest
    command: fastblocks worker
    environment:
      - FASTBLOCKS_ENV=production
      - FASTBLOCKS_LOG_LEVEL=INFO
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317
      - OTEL_SERVICE_NAME=fastblocks-worker
      - DATABASE_URL=postgresql://fastblocks:fastblocks@postgres:5432/fastblocks
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      - otel-collector
      - postgres
      - redis
    networks:
      - fastblocks-network
    restart: unless-stopped

  # ==============================================================================
  # OpenTelemetry Collector
  # ==============================================================================
  otel-collector:
    image: otel/opentelemetry-collector-contrib:latest
    command: --config=/etc/otel-collector-config.yaml
    volumes:
      - ./config/otel-collector-config.yaml:/etc/otel-collector-config.yaml:ro
    ports:
      - "4317:4317"  # OTLP gRPC receiver
      - "4318:4318"  # OTLP HTTP receiver
      - "13133:13133"  # Health check
      - "8888:8888"  # Prometheus metrics
      - "8889:8889"  # Prometheus exporter metrics
      - "9464:9464"  # Zpages
    depends_on:
      - jaeger
      - prometheus
      - elasticsearch
    networks:
      - fastblocks-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:13133"]
      interval: 10s
      timeout: 5s
      retries: 3

  # ==============================================================================
  # Jaeger (Distributed Tracing)
  # ==============================================================================
  jaeger:
    image: jaegertracing/all-in-one:latest
    environment:
      - COLLECTOR_OTLP_ENABLED=true
      - COLLECTOR_OTLP_HOST_PORT=0.0.0.0:4318
    ports:
      - "16686:16686"  # Jaeger UI
      - "14250:14250"  # OTLP gRPC
    networks:
      - fastblocks-network
    restart: unless-stopped

  # ==============================================================================
  # Prometheus (Metrics Storage)
  # ==============================================================================
  prometheus:
    image: prom/prometheus:latest
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=200h'
      - '--web.enable-lifecycle'
    volumes:
      - ./config/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    ports:
      - "9090:9090"  # Prometheus UI
    networks:
      - fastblocks-network
    restart: unless-stopped

  # ==============================================================================
  # Grafana (Metrics Visualization)
  # ==============================================================================
  grafana:
    image: grafana/grafana:latest
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana-data:/var/lib/grafana
      - ./config/grafana/dashboards:/etc/grafana/provisioning/dashboards:ro
      - ./config/grafana/datasources:/etc/grafana/provisioning/datasources:ro
    ports:
      - "3001:3000"  # Grafana UI (different port to avoid conflicts)
    depends_on:
      - prometheus
    networks:
      - fastblocks-network
    restart: unless-stopped

  # ==============================================================================
  # Elasticsearch (Log Storage)
  # ==============================================================================
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.11.0
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    ulimits:
      memlock:
        soft: -1
        hard: -1
    volumes:
      - elasticsearch-data:/usr/share/elasticsearch/data
    ports:
      - "9201:9200"  # Elasticsearch API (different port)
    networks:
      - fastblocks-network
    restart: unless-stopped

  # ==============================================================================
  # Kibana (Logs Visualization)
  # ==============================================================================
  kibana:
    image: docker.elastic.co/kibana/kibana:8.11.0
    environment:
      - ELASTICSEARCH_HOSTS=http://elasticsearch:9200
    ports:
      - "5602:5601"  # Kibana UI (different port)
    depends_on:
      - elasticsearch
    networks:
      - fastblocks-network
    restart: unless-stopped

  # ==============================================================================
  # PostgreSQL (Primary Database)
  # ==============================================================================
  postgres:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=fastblocks
      - POSTGRES_PASSWORD=fastblocks
      - POSTGRES_DB=fastblocks
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - fastblocks-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fastblocks"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ==============================================================================
  # Redis (Cache & Session Storage)
  # ==============================================================================
  redis:
    image: redis:7-alpine
    command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redis-data:/data
    ports:
      - "6379:6379"
    networks:
      - fastblocks-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

# ==============================================================================
# Networks
# ==============================================================================
networks:
  fastblocks-network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.29.0.0/16

# ==============================================================================
# Volumes
# ==============================================================================
volumes:
  prometheus-data:
    driver: local
  grafana-data:
    driver: local
  elasticsearch-data:
    driver: local
  postgres-data:
    driver: local
  redis-data:
    driver: local
